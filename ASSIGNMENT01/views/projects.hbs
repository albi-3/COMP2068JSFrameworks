<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
</head>
<body>
  {{> nav}}

  <h1>{{pageHeader}}</h1>
  <ul>
    {{#each projects}}
      <li>
        <h2>{{this.title}}</h2>
        <p>{{this.description}}</p>
        <a href="{{this.url}}" target="_blank">View on GitHub</a>
      </li>
    {{/each}}
  </ul>

  {{> footer}}
</body>
</html>